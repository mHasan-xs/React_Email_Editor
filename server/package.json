{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 9000"
  },
  "author": "Anik",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
